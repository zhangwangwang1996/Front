// initial state
const state = {
  options: [{
    value: '1',
    label: '黄金糕',
  }, {
    value: '2',
    label: '豆腐脑',
  }],
  disabledOptions: {
    0: '正常',
    1: '禁用',
  },
}

// getters
const getters = {}

// actions
const actions = {}

// mutations
const mutations = {}

export const demo = {
  namespaced: true,
  state,
  getters,
  actions,
  mutations,
}
