// export const MUTATIONS = (state, params) => {
//   state.params = params
// }

export const SET_CLIENT_HEIGHT = (state, value) => {
  state.clientHeight = value
}

export const SET_CLIENT_WIDTH = (state, value) => {
  state.clientWidth = value
}
